# Production Environment Variables for Vercel
# Copy these to your Vercel project environment settings
# Node.js 20.x is required for Vercel deployment

# Required: JWT Secret for authentication
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters

# Application environment
NODE_ENV=production

# Database configuration (Vercel uses file system for SQLite)
DATABASE_PATH=/tmp/bd-ticketpro.db

# Optional: Enable debug logging (set to false in production)
DEBUG=false

# CORS settings (add your production domain)
CORS_ORIGINS=https://your-app-name.vercel.app,https://your-custom-domain.com

# Optional: Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
